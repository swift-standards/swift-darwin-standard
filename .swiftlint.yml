# SwiftLint configuration for swift-kernel-primitives
# Inherits from the shared parent configuration in swift-standards

parent_config: https://raw.githubusercontent.com/swift-standards/swift-standards/main/.swiftlint.yml

# Package-specific overrides (duplicates parent for CI reliability)
disabled_rules:
  - function_parameter_count
  - identifier_name
  - line_length
  - trailing_comma
  - nesting
  - for_where
  - type_name
